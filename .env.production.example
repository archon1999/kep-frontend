# Public build-time endpoints. Copy this file to .env.production locally.
VITE_API_URL=https://api.example.com
VITE_WS_URL=wss://api.example.com/ws

# Never put credentials in a VITE_* variable: Vite exposes these values to the browser bundle.
